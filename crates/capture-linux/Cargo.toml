[package]
name = "capture-linux"
version = "0.1.0"
edition = "2024"

[dependencies]
capture = { path = "../capture" }
libc = "0.2"
zbus = { version = "5.14.0", features = ["blocking-api"] }

[dev-dependencies]
glib = "0.18"
gdkwayland-sys = "0.18"
gtk = "0.18"
