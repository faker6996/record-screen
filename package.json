{
  "name": "record-screen",
  "private": true,
  "version": "0.1.35",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node scripts/run-tauri.js dev",
    "dev:web": "npm run dev --workspace @record-screen/desktop",
    "build": "node scripts/run-tauri.js build",
    "build:web": "npm run build --workspace @record-screen/desktop",
    "lint": "npm run lint --workspace @record-screen/desktop",
    "test:e2e:web": "npm run test:e2e --workspace @record-screen/desktop"
  }
}
